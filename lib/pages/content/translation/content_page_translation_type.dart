enum ContentPageTranslationType {
  hi('hi'),
  yourId('yourId'),
  logoutButton('logoutButton'),
  confirmLogoutTitle('confirmLogoutTitle'),
  confirmLogout('confirmLogout'),
  cancel('cancel');

  final String key;

  const ContentPageTranslationType(this.key);
}
